/* js/slider.js (Optional Cleanup) */
document.addEventListener('DOMContentLoaded', () => {
    const container = document.querySelector('.cert-slider-container');
    if (container) {
        console.log("Auto-slider initialized.");
    }
});